<% if not @generic_file.nil? %>
<%= f.label :subject_hierarchical_geographics %>
<% (@generic_file.subject_hierarchical_geographics + [MODS::MADS::HierarchicalGeographic.new]).each do |subject| %>
	<%= f.simple_fields_for :subject_hierarchical_geographics, subject do |builder| %>
		<%= render_edit_field_partial(:label, {f: builder, label_text: false}) %>
	<% end %>
<% end %>

<% end %>


