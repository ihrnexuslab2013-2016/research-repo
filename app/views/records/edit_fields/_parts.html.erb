<% if not @generic_file.nil? %>
<% if defined? props %>
<% @clabel = props[:label]  %>
<% else %>
<% @clabel = nil? %>
<% end %>

<%= f.input :parts, label: @clabel, as: :nested_attribute_multiple, :fields => [:part_order, :part_level, :part_caption, :part_number], :label_html => { :class => 'multi-part-heading' } %>

<% end %>


