<% if not @generic_file.nil? %>
<%= f.label :note_groups %>
<% (@generic_file.note_groups + [MODS::NoteGroup.new]).each do |note| %>
	<%= f.simple_fields_for :note_groups, note do |builder| %>
		<%= render_edit_field_partial(:label, {f: builder, label_text: false}) %>
	<% end %>
<% end %>

<% end %>


