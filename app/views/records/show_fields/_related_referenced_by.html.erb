<% @generic_file.related_referenced_by.each do |host| %>
  <span itemprop="related_referenced_by" itemscope itemtype="http://schema.org/Thing">
    <span itemprop="name">
    	<%= link_to sufia.generic_file_path(host) do %>
    		"<%= host.title_principals.first.label %></a>" uploaded by <%= host.depositor %>
    	<% end %>		
    </span>
  </span>
  <br />
<% end %>

