<% if not @generic_file.nil? %>
<%= f.label :title_principals %>
<% (@generic_file.title_principals + [MODS::MADS::Title.new]).each do |title| %>
	<%= f.simple_fields_for :title_principals, title do |builder| %>
		<%= render_edit_field_partial(:label, {f: builder, label_text: false}) %>
	<% end %>
<% end %>

<% end %>


